apiVersion: v1
kind: ConfigMap
metadata:
  name: airpulse-config
  namespace: default
  labels:
    app.kubernetes.io/name: airpulse
    app.kubernetes.io/component: serving
data:
  PORT: "8000"
  ENV: "production"
  LOG_LEVEL: "INFO"
  DATA_DIR: "/app/data"
  MODEL_NAME: "airpulse-forecaster"
  STAGE: "Production"
